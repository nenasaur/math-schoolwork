//área de um retângulo//

/*--------------------------------------------------------------------------------

Para calcular a área de um retângulo é simples, apenas base vezes a altura.
onde ;

h = altura
b = base

-------------------------------------------------------------------------------*/

b =prompt("digite a medida da base")
h=prompt("digite a medida da altura")

a=parseFloat(b*h);

alert("a área do retângulo é" + a)
